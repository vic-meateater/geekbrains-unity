using UnityEngine;

namespace Asteroids
{
    public class ShootingShip:IShoot
    {
        public void Shoot()
        {
            //var temAmmunition = Instantiate(_bullet, _barrel.position, _barrel.rotation);
            //temAmmunition.AddForce(_barrel.up * _force);
        }
    }
}